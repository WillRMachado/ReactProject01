const server = require('./config/server') //coloca o server no ar
const db = require('./config/db')
const routes = require('./config/routes') //faz o node-restfull fununciar e o express

// routes(server)